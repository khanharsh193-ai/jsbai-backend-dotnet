{
  "ConnectionStrings": {
    // SQLite database file — will be created automatically in the app folder
    "DefaultConnection": "Data Source=jsbai.db"
  },

  // ⚠ FILL THESE IN BEFORE DEPLOYING
  "AdminPassword": "jsbai-admin-2025",

  "Email": {
    // Your Gmail address
    "SenderEmail": "your.email@gmail.com",

    // Gmail App Password (NOT your regular Gmail password)
    // How to get it: Google Account → Security → 2-Step Verification → App passwords
    // Generate one for "Mail" → copy the 16-character code
    "AppPassword": "your-gmail-app-password",

    // Where editor notifications are sent (can be same as SenderEmail)
    "EditorEmail": "your.email@gmail.com"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
